//
// Created by sebba on 2025-02-17.
//

#include "GameController.h"
/**
void generateRandomGrid(vector<vector<CellType>>& grid) {
    srand(time(0));
    for (int i = 0; i < GRID_SIZE; i++) {
        for (int j = 0; j < GRID_SIZE; j++) {
            int randTile = rand() % 6;
            grid[i][j] = static_cast<CellType>(randTile);
        }
    }
}**/
